# Prometheus configuration for MCP Tool Gateway
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # MCP Gateway metrics
  - job_name: 'mcp-gateway'
    static_configs:
      - targets: ['gateway:8787']
    metrics_path: '/metrics'
    scrape_interval: 10s
